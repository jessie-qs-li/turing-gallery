export type LiteraturePair = {
  id: string;
  humanTitle: string;
  humanSource: string;
  humanExcerpt: string;
  aiModel: string;
  aiPrompt: string;
  aiExcerpt: string;
  explanation: string;
};
